<p>ローケルの文言(:welcome_yaml)　<%= I18n.t('welcome') %></p>

<p>言語 <%= I18n.locale %></p>
<p>文言(:welcome_json)　<%= @localizedStrings["welcome"] %></p>



<ul>
  <% @messages.each do |message| %>
    <li><%= link_to message.id, message_path(message) %> : <%= message.content %></li>
  <% end %>
</ul>